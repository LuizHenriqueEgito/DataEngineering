# Instalação
```bash
# isso instala e já roda
uvx apache-airflow standalone

# ele vai pedir a senha faça assim:
# isso vai mostrar a senha gerada
cat ~/airflow/simple_auth_manager_passwords.json.generated
```
